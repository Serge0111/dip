import { LowAnimationDirective } from './low-animation.directive';

describe('LowAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new LowAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
